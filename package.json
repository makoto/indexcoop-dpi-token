{
  "name": "indexcoop-dpi-token",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ makoto/indexcoop-dpi-token",
    "create-local": "graph create --node http://localhost:8020/ makoto/indexcoop-dpi-token",
    "remove-local": "graph remove --node http://localhost:8020/ makoto/indexcoop-dpi-token",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 makoto/indexcoop-dpi-token"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.17.1",
    "@graphprotocol/graph-ts": "0.17.0"
  }
}
